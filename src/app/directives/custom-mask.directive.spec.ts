import { CustomMaskDirective } from './custom-mask.directive';

describe('CustomMaskDirective', () => {
  it('should create an instance', () => {
    const directive = new CustomMaskDirective();
    expect(directive).toBeTruthy();
  });
});
